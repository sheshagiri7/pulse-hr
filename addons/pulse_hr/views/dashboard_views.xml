<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- TODO (Person 3): build the role-based dashboard here.
         Kanban view on hr.employee showing:
         - employee photo, name, department
         - status dot color (green=present, gray=leave, yellow=absent)
         - check-in/check-out button (link to hr_attendance action)

         Example starter kanban skeleton:

    <record id="pulse_hr_employee_kanban" model="ir.ui.view">
        <field name="name">pulse.hr.employee.kanban</field>
        <field name="model">hr.employee</field>
        <field name="arch" type="xml">
            <kanban class="o_hr_employee_kanban">
                <field name="name"/>
                <field name="login_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card">
                            <strong><field name="name"/></strong>
                            <div><field name="login_id"/></div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>
    -->
</odoo>
